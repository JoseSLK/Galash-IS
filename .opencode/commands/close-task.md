---
description: Verifies a task and records completed implementation work.
agent: implementer
---

Task:

$ARGUMENTS

Run relevant checks. Review changed files. Report exact test results. Append one completed-work event to `docs/bitacora.ndjson`. Update `docs/contexto.md` only if implemented system state changed. Never rewrite historical log entries.
