---
description: Appends one completed-work event to the technical log.
agent: implementer
---

Append one valid JSON object to `docs/bitacora.ndjson` for:

$ARGUMENTS

Use fields `id`, `ts`, `actor`, `type`, `scope`, `summary`, `files`, `tests`, and `status` when applicable. Never edit or delete existing lines. Do not record private requirements, credentials, or personal data.
