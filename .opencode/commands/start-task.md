---
description: Loads project context and starts a scoped implementation task.
agent: implementer
---

Task supplied by user:

$ARGUMENTS

Classify supplied scope first. Read `AGENTS.md`. Read `docs/contexto.md`, logs, and code only when relevant to supplied scope. For testing tasks, read `docs/testing/README.md` first. Filter logs and read no more than 15 matching entries per log file. Implement only supplied scope. Do not read or recreate private requirement documents.
